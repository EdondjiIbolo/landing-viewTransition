---
export interface Props {
    id:string,
    title:string,
    img:string
}
const { title ,  img , id } = Astro.props
---


<a class='hover:scale-105 inline-block transition-all hover:contrast-125 hover:shadow-2xl' href=`/${id}`>
    <img style={`view-transition-name: book-${id}`} 
    class='md:aspect-[389-500]  h-full object-cover max-w-full w-full rounded' 
    src=`${img}` 
    alt=`Portada del Libro de ${title}`>
</a>